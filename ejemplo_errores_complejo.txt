// Ejemplo con errores semánticos y warnings

int varGlobal;

int funcionConError() {
    int x;
    int y;
    
    // ERROR: variable 'z' no declarada
    z = 10;
    
    // WARNING: variable 'y' declarada pero no usada
    
    return x;
}

int main() {
    int contador;
    
    // ERROR: variable 'noExiste' no declarada
    int resultado = noExiste + 5;
    
    // ERROR: función 'funcionNoDeclarada' no existe
    funcionNoDeclarada(10);
    
    // ERROR: cantidad de parámetros incorrecta
    funcionConError(1, 2);
    
    return 0;
}
